<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
     <!-- socket io -->
     <script src="socket.io/socket.io.js"></script>
     <title>Welcome to peerTalk</title>
    <!-- Custom CSS -->
    <style>
        body {
            background: linear-gradient(to right, #74ebd5, #9face6); /* Attractive gradient background */
            font-family: 'Arial', sans-serif;
        }

        h1 {
            background-color: #4A90E2;
            color: white;
            padding: 10px;
            border-radius: 10px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
        }

        #chat-box {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.15);
            padding: 20px;
            margin-top: 20px;
        }

        #chat {
            display: flex;
            flex-direction: column; /* Stack messages vertically */
            align-items: flex-start; /* Default alignment to the left */
            background: #f7f7f7;
            border-radius: 10px;
            padding: 10px;
            height: 300px;
            overflow-y: auto;
        }
        
        #chat li {
            list-style-type: none;
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 20px;
            word-wrap: break-word;
            max-width: 60%;
        }
        #chat li:nth-child(odd) {
            background-color: #d1e7dd;
            color: #1b4332;
        }

        #chat li:nth-child(even) {
            background-color: #f8d7da;
            color: #842029;
        }

        #inp {
            border: 2px solid #4A90E2;
            border-radius: 10px;
        }

        #send-btn {
            background-color: #4A90E2;
            color: white;
            border-radius: 10px;
            transition: background-color 0.3s;
        }

        #send-btn:hover {
            background-color: #4179b8;
        }
    </style>
    <title>Mann ki Baat</title>
</head>
<body>
    <div class="container text-center">
        <h1 class="display-4 mt-4">Mann ki Baat</h1>
        <section id="chat-box" class="mx-auto" style="width: 400px;">
            <ul id="chat" class="ps-0">
                <!-- Messages will appear here dynamically -->
            </ul>
            <textarea class="form-control mt-3" id="inp" rows="3" placeholder="Start typing..."></textarea>
            <button class="btn btn-md mt-3" id="send-btn">Send</button>
        </section>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <!-- Your Script -->
    <script src="/script.js"></script>
</body>
</html>
